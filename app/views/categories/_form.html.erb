<%= form_with model: category do |form| %>
    <p><%= form.label :name, "Название категории" %></p>
    <p><%= form.text_field :name, class: 'form-text-field' %></p>
  
    <p class="mt"><%= form.submit 'Сохранить категорию', class: 'button button-main'%></p>
  <% end %>