<div class="container mt-sm">
  <h2 class="title-lg ">Все категории</h2>
</div>

<% @categories.each do |category| %>
    <div class="mt-sm container">
    <%= render category %> 
    </div>
<% end %>

<div class="container">
    <p class="mt">
      <%= link_to "Создать новую категорию", new_category_path, class: 'button button-main' %>
    </p>
</div>