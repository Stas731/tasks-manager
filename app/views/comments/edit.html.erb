<h1>Редактировать комментарий</h1>

<%= form_for [@task, @comment] do |f| %>
  <p>
    <%= f.label :content, "Содержание" %><br>
    <%= f.text_area :content %>
  </p>

  <p>
    <%= f.submit "Обновить комментарий" %>
  </p>
<% end %>

<%= link_to 'Назад', task_path(@task) %>