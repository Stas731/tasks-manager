<%= form_with model: tag do |form| %>
    <p><%= form.label :name, "Название тега" %></p>
    <p><%= form.text_field :name, class: 'form-text-field' %></p>
  
    <p class="mt"><%= form.submit 'Сохранить тег', class: 'button button-main'%></p>
  <% end %>